# Runtime-only dependencies — for serving pre-trained models and the dashboard.
# Training deps (torch, gpytorch, earthengine-api, etc.) are NOT required here.

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# Dashboard
dash>=2.16.0
dash-bootstrap-components>=1.5.0
plotly>=5.20.0

# Geospatial (rasterio needs system libgdal — see Dockerfile)
rasterio>=1.3.9
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0

# ML inference (loading pre-trained CatBoost + MAPIE artifacts)
catboost>=1.2.3
mapie>=0.8.0
scikit-learn>=1.4.0

# Utilities
tqdm>=4.66.0
loguru>=0.7.2
