node_modules/
.env
__pycache__/
*.pyc
*.pyo

# Virtual environment
.venv/

# Claude internal files
.claude/
.claudeignore

# Dev scratch files
IGNORE/
TASKS.md

# CatBoost training logs
catboost_info/

# Large raw satellite imagery (download via 01_data_ingest.py)
data/raw/sentinel2/
data/raw/dem/
data/raw/planet/

# Large intermediate rasters (regenerated by 02_feature_engineering.py)
data/processed/feature_stack_*.tif

# Keep: prediction maps, model artifacts, training CSVs, feature names
# data/processed/maps/     ← committed (50 GeoTIFFs, ~4 MB each)
# data/models/             ← committed (50 MB model artifacts)
# data/processed/*.csv     ← committed (training data)
# data/processed/*.json    ← committed (feature names)
